/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.nomadavzla.com",
      "placehold.co",
      "nomada-admin.test",
      "nomadavzla.store",
    ]
  }
}

module.exports = nextConfig
